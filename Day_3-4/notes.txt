The hex code( e.g. #534b4b - divide into 2 pairs R G B  )is a *unique identifier for one specific color.* Every color has its own hex code and there are *16^6 combination (= 16,777,216 total colors) 

use color picker for colors

--> learn about auto format code and dev tools

- Duplicate the code

Working with the global CSS & CSS Selectors

   p {
    color: #534b4b
   }


The Anatomy of a Valid HTML Document(page)
<!doctype html> -- version -- HTML5 - 2014
page metadata -- <head></head>
page content  -- <body></body>



VS code shortcuts and Tricks

tab - add multiple blanks , tab for auto-completion
emmet - built in plug-in
select and drag or by press alt and use 

void elements  - link, img < > or < /> -- self closing tag
rel -- relation

id - unique identifier

CSS sizes and units:

for CSS properties that  expect a size (dimensions), you got different options for defining that size

px -- A device-independent pixel on the screen ( a.low resolution device,eg: low budget smartphone . b. High resolution device eg:top tier smartphone )
absolute      relative (rem, % )
